on: ["push"]
name: Test
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: echo
        run: mkdir include && echo "hi" >> include/file.txt && pwd && find /home/runner/work/cicdtest/cicdtest/include
      - name: docker build
        run: docker build . -t test && docker run test find /app/