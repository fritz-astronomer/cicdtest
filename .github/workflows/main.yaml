on: ["push"]
name: Test
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: action/checkout@v2
      - name: echo
        run: mkdir include && echo "hi" >> file.txt
      - name: docker build
        run: docker build .